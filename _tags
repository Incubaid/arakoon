true: debug
true: annot
true: package(lwt)
true: package(lwt.preemptive)
true: package(lwt.unix)
true: package(oUnit)
true: package(str)
true: package(dynlink)
true: package(baardskeerder)
"doc": include
"src":include
"src/otc":include
"src/hope":include
"src/tools":include
"src/client":include
"src/toy":include
"src/msg":include
"src/paxos": include
"src/tlog": include
"src/tlog/tlogcommon.ml": use_macro
"src/node": include
"src/system": include
"src/inifiles": include
"src/nursery": include
"src/plugins": include
"examples/ocaml": include
"src/main" : include
<**/arakoon.*{byte,native}>: is_main
<**/nursery.*{byte,native}>: is_main,use_thread
<**/profile.*{byte,native}>: is_main, package(ocamlviz)
<**/bs_bench.*{byte,native}>: is_main, use_thread
<**/barakoon*.*{byte,native}>: is_main, use_thread
<**/hope_demo.*{byte,native}>: is_main, use_thread
<**/mp_demo.*{byte,native}>: is_main, use_thread
<**/*test.*{byte,native}>: is_main, use_thread
<examples/ocaml/*.{bytes,native}>: use_thread
<examples/ocaml/*.{byte,native}>: is_main
<src/arakoon_client.*{cma,cmxa}>: use_thread
<src/plugins/*.{cm*,cmx*}>: use_thread
