true: debug
true: annot
true: package(lwt)
true: package(lwt.preemptive)
true: package(lwt.unix)
true: package(oUnit)
true: package(bz2)
true: package(str)
true: package(camltc)
true: package(dynlink)
"doc": include
"src":include
"src/tools":include
"src/client":include
"src/toy":include
"src/msg":include
"src/paxos": include
"src/tlog": include
"src/tlog/tlogcommon.ml": use_macro
"src/node": include
"src/system": include
"src/inifiles": include
"src/nursery": include
"src/plugins": include
"examples/ocaml": include
"src/main" : include
<**/arakoon.*{byte,native}>: is_main
<**/nursery.*{byte,native}>: is_main,use_thread
<**/profile.*{byte,native}>: is_main, package(ocamlviz)
<**/bs_bench.*{byte,native}>: is_main, use_thread
<examples/ocaml/*.{bytes,native}>: use_thread
<examples/ocaml/*.{byte,native}>: is_main
<src/arakoon_client.*{cma,cmxa}>: use_thread
<src/plugins/*.{cma,cmxa}>: use_thread

"src/client/client_protocol.ml" : use_log_macro
"src/msg/tcp_messaging.ml" : use_log_macro
"src/paxos/master.ml" : use_log_macro
"src/paxos/slave.ml" : use_log_macro
"src/paxos/multi_paxos.ml" : use_log_macro
"src/paxos/multi_paxos_fsm.ml" : use_log_macro
"src/paxos/multi_paxos_test.ml" : use_log_macro
